{
    "description": "Runtime dependencies bundled for TYPO3 Classic Mode installations (TER package). Composer-based installations resolve these from the root composer.json instead. Only packages NOT already shipped by TYPO3 Core belong here - everything the Core provides is listed under 'replace' to keep it out of Libraries/vendor and avoid a duplicate autoloader registration.",
    "license": "GPL-2.0-or-later",
    "config": {
        "platform": {
            "php": "8.1.0"
        },
        "classmap-authoritative": true
    },
    "require": {
        "mautic/api-library": "^3.1"
    },
    "replace": {
        "guzzlehttp/psr7": "*",
        "leuchtfeuer/marketing-automation": "*",
        "psr/http-client": "*",
        "psr/http-factory": "*",
        "psr/http-message": "*",
        "psr/log": "*",
        "ralouphie/getallheaders": "*",
        "symfony/deprecation-contracts": "*",
        "symfony/polyfill-php80": "*"
    }
}
